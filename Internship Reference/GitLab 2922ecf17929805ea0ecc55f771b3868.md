# GitLab

## How to create Git Repository

1. Create a dir
2. cd to the dir
3. type in within the dir git status → If that doesnt work type in → git init
4. then type in git status

## Configure Git

```bash
git cofnig --global user.name "Musie Gherzghir"
git cofnig --global user.email "mussiegherzghir"
```

Next is your first git commit

```bash
#First create a file using vi edit

vi READ.md

"hello my name is musie"

#save the file

git status

git add <filename>

git status

git commit -m "hello lets do this"

```

## Viewing git log

```bash
git log

#for more details log
git log --patch
```